# 隐私与数据说明

适用版本：0.3.5。Quotile 是独立的手机本地客户端，没有项目自建的账号服务器、分析统计或广告 SDK。

## 手机上保存什么

| 数据 | 用途与保存方式 |
| --- | --- |
| 登录令牌、账号标识和显示名 | 用于读取当前账号额度；令牌及账号凭据由 Android Keystore 支持的 AES-GCM 加密，保存在应用私有目录 |
| 最近一次额度、重置时间、可用重置次数、最近到期时间和读取时间 | 用于桌面离线显示；额度快照保存在应用私有设置中，不是加密令牌库的一部分 |
| 主题、演示模式和自动刷新设置 | 保存在应用私有设置中 |

Android 备份在应用清单中关闭。额度及时间会显示在桌面，因此看到你的桌面或截图的人也可能看到这些信息。

为确定最近到期时间，应用会在内存中解析重置机会明细，只保存用于显示的时间，不保存原始明细、机会 ID 或完整接口响应。

## 什么时候联网

- 你主动登录时：通过 OpenAI 的授权页面及 HTTPS 接口完成授权。密码在官方浏览器页面输入，Quotile 不读取密码或浏览器 Cookie。
- 你点击刷新时：手机直接请求 `chatgpt.com/backend-api/wham/usage` 的 Codex 额度接口；可用重置次数大于 0 时，在同一次刷新中额外读取 `chatgpt.com/backend-api/wham/rate-limit-reset-credits`，查询最近到期时间。需要时在这次操作中向 `auth.openai.com` 续期登录令牌。
- 你主动开启自动刷新后：系统按所选 15／30／60 分钟间隔尝试执行相同读取，实际时刻可能受系统和网络影响。
- 设备码登录期间：仅在本次登录会话内轮询授权状态；完成、取消或超时后结束。

**自动刷新默认关闭。** 关闭后，打开应用、添加/缩放小组件或保存外观不会额外查询额度。系统仍可能启动应用组件以重绘桌面或取消历史任务，这不等于后台查询额度。

到期明细查询有单独的短时限，失败时保留本次成功读取的主要额度，并将期限显示为未提供。次数为 0 或未知时不发起这项查询。缓存时间到期不会触发额外联网；应用不会调用消耗重置机会的接口。

读取会向 OpenAI 发送必要的登录令牌、账号标识与客户端标识。官方服务和系统浏览器如何处理网络请求，适用它们各自的隐私政策。
这些信息不会发送给项目维护者或 GitHub。构建与测试只使用合成数据。

## 权限用途

| Android 权限 | 用途 |
| --- | --- |
| Internet | 登录与读取额度 |
| Network state | 配合网络状态与系统任务执行 |
| Receive boot completed | 重启后按已保存的开关恢复自动刷新安排，或清理不应存在的任务 |

没有通讯录、短信、定位、麦克风或相机权限；不需要 Root，也不读取 ChatGPT App 的私有数据。

## 删除和反馈

「退出登录」清除本机保存的登录凭据与额度快照并更新小组件。它只执行本地退出，不向 OpenAI 发送远程令牌吊销请求，也不退出系统浏览器中的 ChatGPT。清除应用数据或卸载会删除应用本地数据；远程会话应在 OpenAI 账号侧管理。

提交问题前请遮住邮箱、账号标识及个人桌面内容。不要上传令牌、Cookie、设备验证码、完整授权链接、私钥、口令文件或原始账号接口响应。通常应用版本、手机型号、系统版本、现象与脱敏截图就足够定位问题。

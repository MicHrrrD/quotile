# 验证记录 · 2026-09-05

## 已完成

- Android SDK 云构建：GitHub Actions [33953005310](https://github.com/MicHrrrD/quotile/actions/runs/33953005310) 成功；源码提交 `b4e7367f7dd746785af9d2c9c82dcabea7afdd45`。Java 类型检查、Android 资源链接、DEX、调试签名配置检查与 APK 打包通过。
- 下载核验：GitHub ZIP 的 SHA-256 与 Actions 返回的摘要一致；ZIP 与内部 APK 的 CRC 全部通过；包含 Manifest、DEX、资源和 APK 签名块。APK 为 74,465 字节，SHA-256 为 `d8d5e82ac394e1421adda6c4dc8ac39fed2172bad78652ec3fe65106b4590db7`。未另外使用 apksigner 复核签名。
- Android 工程：12 个 Java 文件通过 Java 17 编译器的 **parse 语法检查**；9 个 XML 可解析，本地资源引用、5 个 manifest 组件、API／Gradle声明及两类小组件网格元数据通过静态检查。
- 布局设计：检查浅色与深色的 350×64dp、350×150dp、160×150dp、110×64dp 预览；均清楚标记为设计预览和演示数据。
- Python 镜像布局：4536 个尺寸／状态／数值组合的文字横向及底部边界检查通过；状态修正后另测 294 例。使用的字体度量不等同于 Android，不能作为启动器兼容性证明。
- 桥接服务：41 个离线测试通过，覆盖周与五小时窗口辨认、可选窗口缺失、异常数值、持久 JSON-RPC 握手、进程超时回收和重启、拒绝上游额外回调、并发请求合并、缓存、鉴权、限流、配置权限、失败保留快照与原更新时间。
- 人工复核：修复了固定30分钟的系统更新与所选周期重复调度；将声明的可调最大尺寸与布局支持范围对齐；未配置时刷新按钮直接打开配置页，避免后台广播跳转限制。

## 尚未完成

- APK 在手机上的安装与启动验证。
- Fold7 的 One UI 实机测试，包括内外屏、网格缩放、横竖屏、系统字体和显示缩放、桌面组件拖动、重启恢复、休眠／省电与断网恢复。
- 用户真实账号的额度读取、额度服务部署和手机配对。

## 判断范围

后续修订：较高卡片仅在账号实际返回五小时额度时显示双栏；没有该项时使用周额度专用布局。此修订已包含在通过编译的 APK 中，未进行 Android 渲染验证；已有 PNG 仍是含两种额度的演示情景。

当前 APK 可供个人试装与后续配对，**不能描述为已通过 Fold7 实机测试或已验证能读取此账号的额度**。PNG 是设计预览，源码 ZIP 不是 APK。

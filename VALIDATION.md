# 验证记录 · 2026-09-05

## 已完成

- Android 工程：12 个 Java 文件通过 Java 17 编译器的 **parse 语法检查**；9 个 XML 可解析，本地资源引用、5 个 manifest 组件、API／Gradle声明及两类小组件网格元数据通过静态检查。
- 布局设计：检查浅色与深色的 350×64dp、350×150dp、160×150dp、110×64dp 预览；均清楚标记为设计预览和演示数据。
- Python 镜像布局：4536 个尺寸／状态／数值组合的文字横向及底部边界检查通过；状态修正后另测 294 例。使用的字体度量不等同于 Android，不能作为启动器兼容性证明。
- 桥接服务：41 个离线测试通过，覆盖周与五小时窗口辨认、可选窗口缺失、异常数值、持久 JSON-RPC 握手、进程超时回收和重启、拒绝上游额外回调、并发请求合并、缓存、鉴权、限流、配置权限、失败保留快照与原更新时间。
- 人工复核：修复了固定30分钟的系统更新与所选周期重复调度；将声明的可调最大尺寸与布局支持范围对齐；未配置时刷新按钮直接打开配置页，避免后台广播跳转限制。

## 尚未完成

- Android SDK 编译、Android 类型检查、资源链接、APK 签名和安装。环境缺少 Android SDK，外部依赖下载请求未获放行。
- Fold7 的 One UI 实机测试，包括内外屏、网格缩放、横竖屏、系统字体和显示缩放、桌面组件拖动、重启恢复、休眠／省电与断网恢复。
- 用户真实账号的额度读取、额度服务部署和手机配对。
- 云构建工作流（main 推送或手动触发）的首次运行验证；最新结果以仓库 Actions 为准。

## 判断范围

后续修订：较高卡片仅在账号实际返回五小时额度时显示双栏；没有该项时使用周额度专用布局。此修订通过 Java 语法检查，未进行 Android 渲染验证；已有 PNG 仍是含两种额度的演示情景。

当前结果可供审阅设计、继续编译与接入，**不能描述为已安装可用的小组件或已验证能读取此账号的额度**。PNG 是设计预览，ZIP 是源码交付，均不是 APK。

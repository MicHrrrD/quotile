# 验证记录 · 0.2.0 · 2026-09-05

## 已完成

- 手机原生 OAuth、加密登录缓存、手动额度读取代码已实现。
- 自动刷新改为默认关闭的明确开关；关闭时取消任务和自动读取，打开时使用15／30／60分钟周期。取消旧版任务的迁移逻辑已添加。
- Java17 语法、XML、资源、组件引用和 1×5／2×5 网格元数据通过静态检查。
- 根据固定的 OpenAI 开源客户端版本检查 PKCE、回环回调、额度窗口转换和 HTTPS 请求；独立复核了凭据保存与取消行为。
- [0.2.0 云构建](https://github.com/MicHrrrD/quotile/actions/runs/33957345128) 全部成功，构建源码为 `8341769f3fdfc7b4d237fe502dc1bbf0251d1460`。
- Android SDK 36 编译、资源链接、APK 打包成功；`apksigner verify` 确认 v2 签名有效，包名 `dev.mich.quotile`，versionCode `2`，versionName `0.2.0`。
- Android16 模拟器成功安装并运行测试，原始结果为 `INSTRUMENTATION_CODE: -1`：默认自动刷新 OFF、关闭后取消任务、打开与重开设置保持离线、开关/主题/间隔不丢弃手动读取，以及 24 种 Canvas 卡片绘制组合通过。
- 合成数据测试覆盖 OAuth 回调校验、PKCE、额度窗口解析、Android Keystore 加密存取和篡改拒绝；没有使用真实账号、没有调用上游额度接口。
- 取回的构建压缩包 SHA-256 与 Actions 公布值一致，APK 内部 ZIP 校验通过。

交付文件 `Quotile-0.2.0.apk` 为 120873 字节，SHA-256：

```text
9eece010a10067b5e92d646fdf8dd38b17003b75c85a674fa2633f8605f8bb28
```

调试签名证书 SHA-256：`fb822318d4c146abe4443c1a5707021a379ea762b0995a114e977cd5b6bd5da9`。这是本次构建的调试签名；其他构建可能需要先卸载再安装。

构建过程中修复了云端模拟器磁盘不足，以及测试脚本未使用原始输出导致读取不到结果码的问题。最终链接所指构建的所有步骤均成功。

## 等待验证

- Fold7 实机安装、浏览器登录、账户授权、令牌到期续期和真实额度读取。
- 此账号的 Codex 额度与用户所指 Pro 周限额是否一致。
- One UI 的内外屏、横竖屏、网格缩放、系统字体与显示缩放、桌面组件拖动。

## 边界

OAuth 和额度 HTTP 逻辑根据 OpenAI 开源代码适配，不是公开承诺兼容的第三方 Android SDK。登录 UI、授权范围或后端防护变化可能需要继续调整。云端没有登录用户账号，不以演示或测试数据声称真实额度读取成功。

0.1.0 曾通过 [首次云构建](https://github.com/MicHrrrD/quotile/actions/runs/33953005310)，该结果不能代替新版验证。

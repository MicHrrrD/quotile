# 验证记录 · 0.3.5 · 2026-09-05

## 0.3.5 已确认

- 包名 `dev.mich.quotile`，versionCode `8`，versionName `0.3.5`。
- 详细小组件在「可用重置 N 次」下显示最近一次重置机会的到期时间（北京时间）；次数为 0 或未知时隐藏新增行，单行布局保持原样。
- 次数大于 0 时，在现有读取操作内额外 GET `/backend-api/wham/rate-limit-reset-credits`，最多 4 秒，受原 25 秒总预算约束。辅助错误保留成功的主额度；取消/登出继续生效。
- 仅从与官方摘要次数一致的完整有效明细确定最近期限；截断、重复、坏日期和计数变化不猜测。快照只新增可空时间戳，兼容旧版，不保存机会 ID 或原始明细，不执行兑换。
- 新增解析、条件请求、辅助失败、取消/登出、时区、缓存兼容和清理测试；59 个原生布局用例及 1 个真实 RemoteViews.reapply 回归，检查归零/缩小后旧行消失。
- 应用源码提交 `762e44e72f6202cbdd0798ed9550bf1acd2f4386`，本轮 [GitHub Actions](https://github.com/MicHrrrD/quotile/actions/runs/33972461937) 全部通过。SDK36 编译、Manifest、构建签名与 Android16 模拟器检查成功；原始结果 `INSTRUMENTATION_CODE: -1`。

- 59 个布局用例、1 个含 5 次 reapply 的真实更新回归和 24 个基本渲染通过，输出 35 张原生图片；已目检 350×180dp 单周/双栏/零次数和 350×110dp 放大字号边界图，完整行高且无重叠。测试与预览使用合成数据，真实账号是否返回足够明细以手机刷新结果为准。
- Actions ZIP SHA-256 `0e6fbf6a6dc035289a342f73d0fdeeb7a739fed01450c9991143da0119158a07` 匹配；APK 和官方签名工具通过构建清单散列校验。
- 最终 `Quotile-0.3.5.apk`：165472 字节，SHA-256 `07eec19669185172fca87eb251c33998290c4eb9eac1e996b7b201d6f314f5fc`。固定证书、v2/v3 签名、ZIP CRC 和全部非签名应用内容一致性验证通过。
- 证书 SHA-256 `d2cb32c3211f1d587e5900d646222a7aca046142cb0eb99adb0eb5f7f1f26079` 与此前交付 0.3.x 相同，可覆盖安装保留登录、设置与小组件。升级后手动刷新一次读取新增到期时间。

以下为历史验证记录。

## 0.3.4 已确认

- 包名 `dev.mich.quotile`，versionCode `7`，versionName `0.3.4`，交付名 `Quotile-0.3.4.apk`。
- 两行及以上（实际高度 ≥110dp）在重置时间下方显示账户共用的「可用重置 N 次」，单行保持原样。预留完整字体行高，短卡片合并刷新/旧数据标记，高度充足时保留独立更新时间。
- 仅解析现有 `GET /backend-api/wham/usage` 返回的 `rate_limit_reset_credits.available_count`；非负整数才有效，缺失/异常保持未知。与额度同次保存、同次更新；兼容旧快照，无新增请求和兑换操作。
- [0.3.4 构建](https://github.com/MicHrrrD/quotile/actions/runs/33969014129) 全部通过，应用源码提交 `cc94db70fdd51621043e573bc287cd525c5e84d4`。SDK36 编译、Manifest、构建签名及 Android16 模拟器验证成功，原始结果 `INSTRUMENTATION_CODE: -1`。
- 数据测试覆盖 0、正数、i64 最大值、缺失/null/负数/小数/字符串/溢出，且不会把余额、累计次数或详情列表长度混入；旧快照兼容、持久化、失败刷新保留旧数据及后续未知清除旧次数通过。
- 59 个布局用例与 24 个基本原生渲染通过，覆盖 110/134/142/150/159/160/180/300dp 高度、窄宽、100%/130%/200% 字号、单双栏、0/2/未知和刷新/旧数据/演示状态；数字与底注完整行高、不重叠及一行不显示次数均通过。产物共 33 张原生图片。
- 已目检实际渲染的 350×180dp 每周/双栏样例和 350×110dp 放大字号边界图；次数在重置时间下方，字体与进度条清晰。预览与测试均为合成数据；真实账户是否提供该可选字段需手机刷新确认。
- Actions ZIP SHA-256 `e381ca0922d3f0913dcd4edbf647965d11e61f4b805ed78be8cf34001f178a97` 校验一致；APK 与 Android 官方签名工具均匹配构建清单。
- 最终 `Quotile-0.3.4.apk`：157280 字节，SHA-256 `acd3e8b7a800fa649f1f69c41608e52931070eb71f5161fd84bc2079342f0183`。原固定证书、v2/v3 签名、ZIP CRC 和全部非签名应用内容一致性验证通过。
- 证书 SHA-256 `d2cb32c3211f1d587e5900d646222a7aca046142cb0eb99adb0eb5f7f1f26079` 与此前交付 0.3.x 相同，支持覆盖安装。升级后手动刷新一次读取新增字段；登录及默认关闭的自动刷新语义不变。

以下为历史验证记录。

## 0.3.3 已确认

- 目标包名 `dev.mich.quotile`，versionCode `6`，versionName `0.3.3`，交付名 `Quotile-0.3.3.apk`。
- 用户希望在 2×5 模式下将主要内容稍微下移，保持已认可的整体外观。
- 本次仅调整详细卡片中的百分比、进度条和重置时间：随可用底部留白向下移动，最大 10dp；较矮的卡片会减少位移，避免挤压更新时间。标题、GPT 图标、刷新按钮、1×5 布局及登录和刷新流程保持原样。
- [0.3.3 构建](https://github.com/MicHrrrD/quotile/actions/runs/33963740595) 全部通过，应用源码提交 `a0be23a785a0c2f8927d0bcd63dc8d0c65be6f06`。SDK36 编译、Manifest、构建签名和 Android16 模拟器检查成功，原始结果 `INSTRUMENTATION_CODE: -1`。
- 沿用现有布局检查，增加 350×180dp 的每周/双栏 100%/130% 字号预览；共 24 种基本原生渲染与 17 种底注/字号布局用例，41 张图片。较矮的 142/150dp 卡片底注仍完整且不重叠。
- 已目检 350×180dp 单栏与放大字号双栏原生图。该高度下百分比、进度条、重置时间下移 10dp，标题及更新时间保持原位；实际 One UI 外观仍以用户安装后为准。
- ZIP SHA-256 `3179a714d46c313cb67f41e407b1fc2ba7acaa5175e22b796b112318e4075f0f` 与 Actions 匹配。APK 与签名工具散列匹配构建清单。
- 最终 `Quotile-0.3.3.apk`：157280 字节，SHA-256 `785d482c83e1d9e50e533505d60cdc2e60d94870cf6ea435e91685091082ac30`。原固定证书及 v2/v3 签名校验通过，全部非签名 ZIP 内容与已测试 APK 一致。
- 交付需沿用 0.3.0 固定私钥，证书 SHA-256 应为 `d2cb32c3211f1d587e5900d646222a7aca046142cb0eb99adb0eb5f7f1f26079`，以支持从已交付的 0.3.0 / 0.3.1 / 0.3.2 覆盖安装并保留登录状态和设置。

以下为历史验证记录。

## 0.3.2 已确认

- 目标包名 `dev.mich.quotile`，versionCode `5`，versionName `0.3.2`，交付名 `Quotile-0.3.2.apk`。
- 用户已在 Galaxy Z Fold7 实机确认 0.3.1 的设备码登录成功，并认可当前组件外观。此次反馈为底部时间文字被裁切，另希望添加小 GPT 图标和「Codex 额度」来源标注，保持整体布局。
- 本次范围仅为上述组件显示调整及相应渲染回归检查，不修改账号登录、额度读取或定时刷新行为。
- [0.3.2 构建](https://github.com/MicHrrrD/quotile/actions/runs/33962762493) 全部通过；应用源码提交 `e3ad23ee721244f9e5b544d3633a7de08b5dc1cb`。SDK36 编译、Manifest、构建签名及 Android16 模拟器测试成功，原始结果 `INSTRUMENTATION_CODE: -1`。
- 新增 13 个中文与时间混排底注用例，验证完整原生行高、底部留白、行间及进度条不重叠、Logo 与 Codex 文字及数值不重叠。覆盖 350×64 的 100%/130%/200% 字号、350×142/150 的每周及双栏 100%/130% 字号、110×64 的 100%/200% 字号。原有 24 种控件渲染与登录、手动刷新合约测试也通过。
- 已目检原生 1×5、放大字号 1×5 及双栏 2×5 图片，混排底注下沿完整，来源标记可辨认。交付预览使用合成快照。
- Actions ZIP SHA-256：`c79d3cda32a931d8feb862df6dc6ab82793bb4298a720412d4cd7198c18f6e60`，下载后匹配；APK 与签名工具散列匹配构建清单。
- 最终 `Quotile-0.3.2.apk`：157280 字节，SHA-256 `f99fba110579ac6d903c029ed337bdb1c23d93910ee35b12ae9341f7407f5a2d`。使用原固定签名，v2/v3 与证书验证通过，全部非签名 ZIP 内容与已测试 APK 一致。
- 交付继续沿用 0.3.0 固定私钥，证书 SHA-256 应为 `d2cb32c3211f1d587e5900d646222a7aca046142cb0eb99adb0eb5f7f1f26079`。从已交付的 0.3.0 / 0.3.1 可直接覆盖安装，保留登录状态、设置及小组件，无需卸载或重新登录。
- 文字裁切修复仍需交付后在用户的 One UI 实际尺寸、字体与显示缩放下确认。

以下为 0.3.1 与 0.3.0 的历史验证记录，不作为 0.3.2 已通过验证的依据。

## 0.3.1 已确认

- 目标包名 `dev.mich.quotile`，versionCode `4`，versionName `0.3.1`，交付名 `Quotile-0.3.1.apk`。
- 用户报告 0.3.0 在官方工作空间选择页持续转圈，切回应用仍在等待，3 分钟后超时；多次重试一致。0.2.0 与 0.3.0 的 AccountClient 相同，尚未确认根因。
- 本次增加官方设备码登录备用入口，不依赖浏览器 localhost 回调；最长 15 分钟，可取消，只在用户主动登录期间轮询授权结果。
- 浏览器登录继续保留 3 分钟限制，并增加阶段提示。设备码登录要求账号安全设置允许此方式，官方页面的可用性仍需用户实际验证。
- 登录成功不自动查询额度，手动刷新和默认关闭的定时刷新设置保持原有行为。
- [0.3.1 构建](https://github.com/MicHrrrD/quotile/actions/runs/33961464322) 已通过，应用源码提交为 `a46d7c393352d786cf6fd1b765dd6a0eb73caff2`。SDK36 编译、Manifest 与构建签名校验、Android16 合成测试全部通过，原始结果 `INSTRUMENTATION_CODE: -1`。
- 新测试覆盖设备码字段兼容、403/404 轮询间隔、PKCE/令牌校验、异常终止、取消/退出/两种登录互相替换/超时后的结果不写入，以及不查询额度、不创建定时任务。既有手动模式、24 种原生组件渲染与加密存储测试也通过。本次未使用真实账号授权。
- 产物 ZIP SHA-256 与 Actions 一致：`1ef7a918f7430f59169f818362827b2820f18fe749b9a8dd5b985a8c4f3f6545`。最终 APK 使用原固定签名，v2/v3、证书及全部非签名 ZIP 内容一致性校验通过。
- 最终 `Quotile-0.3.1.apk`：153021 字节，SHA-256 `c1418ec78ef54474b210e8c6b7337c893f7054373ebce4e9097105cd88042d44`。
- 交付时必须沿用 0.3.0 固定私钥，证书 SHA-256 应为 `d2cb32c3211f1d587e5900d646222a7aca046142cb0eb99adb0eb5f7f1f26079`。使用该签名时，已交付的 0.3.0 可原地覆盖升级并保留设置；0.2.0 仍需一次卸载迁移。
- 交付后，用户在 Galaxy Z Fold7 上确认本次设备码登录顺利完成；官方工作空间页此前反复超时的根因仍未确定。

以下为 0.3.0 的历史验证记录。

## 0.3.0 已确认

- 用户已在 Galaxy Z Fold7 上安装 0.2.0，并确认正常运行、登录和读取额度。
- 0.3.0 保留成功使用的 AccountClient、TokenVault 和额度解析接口。
- 原生 RemoteViews 文字、胶囊进度条、矢量图标取代桌面整图缩放。
- 桌面刷新改为显式广播，不启动 Activity、不新建手动 Job；保留默认关闭的自动刷新开关。
- Java17 语法、XML、资源引用和 Manifest 静态检查通过。

## 0.3.0 历史构建

[最终 Android16 构建](https://github.com/MicHrrrD/quotile/actions/runs/33959427790) 全部通过。应用源码提交为 `9a20b7e61cb5e4561ede22921e359cd4f9422865`。

- Android SDK36 编译、资源链接、构建签名通过；包名 `dev.mich.quotile`，versionCode `3`，versionName `0.3.0`。
- 真实广播入口在未登录状态下不启动 MainActivity 或 RefreshActivity、不遗留读取线程、不创建手动定时任务。
- 自动刷新默认关闭、关闭任务取消、主题及间隔不使手动结果失效的检查通过。
- 原生 TextView、ProgressBar 和 24 种尺寸/主题/额度组合渲染通过，包含 1×5 百分比区域确实有文字像素的回归检查。
- 已目检最终紧凑、双栏、窄尺寸截图，修复了单行横向滚动导致右对齐百分比不可见的问题。
- 原始模拟器结果 `INSTRUMENTATION_CODE: -1`；测试没有使用真实账号或请求上游额度接口。
- 取回产物的 SHA-256 与 Actions 一致。使用 Android 官方 apksigner 本地重签后，v2/v3 签名均验证通过，证书与私下保存的固定私钥一致；每个非签名 ZIP 条目的内容校验值与测试 APK 完全一致。

交付 APK：`Quotile-0.3.0.apk`，132541 字节。

```text
SHA-256: 456c9116d8501bd496dd5b6003da6c440d7b0349ac60005300d8291fe394e80b
证书 SHA-256: d2cb32c3211f1d587e5900d646222a7aca046142cb0eb99adb0eb5f7f1f26079
```

本地签名工具已实际运行验证：密钥和密钥库口令使用不同临时文件，避免 apksigner 重用文件读取器导致 EOF；分别验证默认 v3 与指定验证范围下的 v2。私钥及口令不在仓库中。

0.2.0 的临时调试签名无法用于这次升级，需要先卸载旧版；以后应继续使用上述固定签名交付覆盖更新。

## 0.3.0 实机边界

0.3.0 的 One UI 内外屏、缩放、原地刷新体验仍待实机确认；安装后用户报告官方授权页反复等待超时。用户报告的真实额度读取成功来自 0.2.0。数据源仍是 Codex 套餐额度，不能据此推断所有 ChatGPT 模型的独立限额。

0.2.0 [历史构建](https://github.com/MicHrrrD/quotile/actions/runs/33957345128) 已通过编译、签名与模拟器检查。
